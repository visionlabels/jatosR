% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_results_from_text.R
\name{load_results_from_text}
\alias{load_results_from_text}
\title{Load JATOS Results from JSON Text}
\usage{
load_results_from_text(txt, parse_dates = TRUE)
}
\arguments{
\item{txt}{A character string containing JATOS results in JSON format.
Typically obtained from a JATOS API response or JSON file export.}

\item{parse_dates}{Logical. If \code{TRUE} (default), parses date columns to
\code{POSIXct} (via \code{lubridate::as_datetime()}) and duration to
\code{Duration} (via \code{lubridate::as.duration()}).
If \code{FALSE}, keeps them as numeric or character.}
}
\value{
A single-row tibble with columns:
\itemize{
\item \code{api_version}: JATOS API version (character)
\item \code{study_id}: Study ID (numeric)
\item \code{study_uuid}: Study UUID (character)
\item \code{study_title}: Human-readable study title (character)
\item \code{study_results}: List column containing a tibble of study results
(as produced by \code{\link[=load_study_results]{load_study_results()}}), or \code{NULL} if no results
}
}
\description{
Parses JATOS study results from a JSON text string and transforms them into
a tidy tibble format. This is the primary entry point for importing JATOS data.
}
\details{
This function is typically the first step in a data import pipeline:
\enumerate{
\item Parse JSON from text via \code{jsonlite::fromJSON()}
\item Extract study metadata (id, uuid, title)
\item Recursively process nested study results via \code{\link[=load_study_results]{load_study_results()}}
\item Return a single-row tibble with list columns containing nested data
}

The resulting structure allows for further processing and analysis of all
study and component results in a tidy, relational format.
}
\examples{
\dontrun{
demo <- prepare_demo(nofile = TRUE)
txt <- demo$meta |> httr2::resp_body_string()
r1 <- load_results_from_text(txt)
is_valid_result(r1)
}

}
\seealso{
\code{\link[=load_study_results]{load_study_results()}} for details on study result processing;
\code{\link[=is_valid_component_result]{is_valid_component_result()}}, \code{\link[=is_valid_study_result]{is_valid_study_result()}} for validation.
}
