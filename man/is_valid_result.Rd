% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/is_valid_result.R
\name{is_valid_result}
\alias{is_valid_result}
\title{Validate Complete JATOS Results Structure}
\usage{
is_valid_result(rr, verbose = FALSE)
}
\arguments{
\item{rr}{A tibble representing complete JATOS results (typically from
\code{\link[=load_results_from_text]{load_results_from_text()}}).}

\item{verbose}{Logical. If \code{TRUE}, prints information about failed checks.
Default is \code{FALSE}.}
}
\value{
A logical scalar: \code{TRUE} if all validation checks pass, \code{FALSE} otherwise.
}
\description{
Validates whether a JATOS results object has the expected structure, column names,
and data types as returned by \code{\link[=load_results_from_text]{load_results_from_text()}}.
}
\details{
Performs comprehensive validation of a JATOS results object including:
\itemize{
\item Verifies the input is a tibble
\item Checks for required columns: \code{api_version}, \code{study_id}, \code{study_uuid}, \code{study_title}
\item Validates data types: \code{study_id} (integer), \code{study_uuid} and \code{study_title} (character)
\item Validates the \code{study_results} column is a list
\item Validates all nested study results are valid (via \code{\link[=is_valid_study_result]{is_valid_study_result()}})
}

This function validates the complete hierarchical structure: top-level metadata,
study results, and all nested component results.

When \code{verbose = TRUE}, failed checks are reported via \code{cli::cli_alert_info()}.
}
\examples{
\dontrun{
# Validate JATOS results
results <- load_results_from_text(json_text)
is_valid_result(results)

# Show which checks fail
is_valid_result(results, verbose = TRUE)
}

}
\seealso{
\code{\link[=load_results_from_text]{load_results_from_text()}} which creates results that pass this validation;
\code{\link[=is_valid_study_result]{is_valid_study_result()}}, \code{\link[=is_valid_component_result]{is_valid_component_result()}} for nested validation.
}
