% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_metadata_from_text.R
\name{process_metadata_from_text}
\alias{process_metadata_from_text}
\title{Turns JATOS metadata JSON into a single table}
\usage{
process_metadata_from_text(txt)
}
\arguments{
\item{txt}{JSON metadata as text, a URL, or a path to a JSON file — passed
through to \code{jsonlite::fromJSON()}, which accepts any of those forms}
}
\value{
list with
\describe{
\item{\code{meta}}{General info like study ID or API version}
\item{\code{data}}{Tibble with all meta data, one row per study/component result}
}
}
\description{
Parses JATOS metadata (the \code{studyResults}/\code{componentResults} tree) shared
by \code{process_results} and \code{process_results_from_file}, regardless of
whether it came from a live API response or a local export. Only the
metadata is processed here; neither function adds the actual response
data (column \code{data_raw}) until after this returns.
}
\examples{
fn <- system.file("extdata", "srtdemo_jatos_results.zip", package = "jatosR")
tmpfolder <- tempfile()
utils::unzip(fn, exdir = tmpfolder)
r <- process_metadata_from_text(file.path(tmpfolder, "metadata.json"))
r
unlink(tmpfolder)
}
