% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_results_from_file.R
\name{process_results_from_file}
\alias{process_results_from_file}
\title{Turns metadata and data into a single table, from a local results file}
\usage{
process_results_from_file(path)
}
\arguments{
\item{path}{Path to the zip file with results and metadata}
}
\value{
list with
\describe{
\item{\code{meta}}{General info like study ID or study title}
\item{\code{data}}{Tibble with all meta data, column \code{data_raw} stores the results for each entry in text format. This can be parsed for example with \code{jsonlite} or \code{jspsychread} packages}
}
}
\description{
Like \code{process_results}, but reads both metadata and raw data from a single
zip archive already downloaded to disk (e.g. exported from the JATOS web
interface), instead of combining separate \code{get_metadata}/\code{get_results} API
responses. The archive is expected to contain a top-level \code{metadata.json}
plus a data folder per component result, mirroring the layout produced by
JATOS's own results export.
}
\examples{
fn <- system.file("extdata", "srtdemo_jatos_results.zip", package = "jatosR")
r <- process_results_from_file(fn)
r
}
