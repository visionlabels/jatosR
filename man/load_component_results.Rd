% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_component_results.R
\name{load_component_results}
\alias{load_component_results}
\title{Load Component Results from JATOS data JSON}
\usage{
load_component_results(cr, parse_dates = TRUE)
}
\arguments{
\item{cr}{A list containing raw component result data from JSON.}

\item{parse_dates}{Logical. If \code{TRUE} (default), parses date columns to
\code{POSIXct} (via \code{lubridate::as_datetime()}) and duration to
\code{Duration} (via \code{lubridate::as.duration()}).
If \code{FALSE}, keeps them as numeric or character.}
}
\value{
A single-row tibble with columns:
\itemize{
\item \code{id}: Component result ID (numeric)
\item \code{component_id}: Component ID (numeric)
\item \code{component_uuid}: Unique component identifier (character)
\item \code{start_date}: Start time (POSIXct or numeric, depending on \code{parse_dates})
\item \code{end_date}: End time (POSIXct or numeric, depending on \code{parse_dates})
\item \code{duration}: Elapsed time (Duration or character, depending on \code{parse_dates})
\item \code{component_state}: Component state/status (character)
\item \code{path}: Study path (character)
\item \code{data_size}: Data size in bytes (numeric)
\item \code{data_size_human_readable}: Formatted data size (character)
\item \code{files}: List column containing tibble of file data or \code{NULL}
\item \code{is_quota_reached}: Whether quota was reached (numeric, 0/1)
}
}
\description{
Transforms raw JATOS component result data into a tidy tibble format,
handling nested structures and optional date parsing.
It expects the results were imported from JSON using
\code{jsonlite::fromJSON(json, simplifyVector = F)}.
}
\details{
Missing values in the input are filled with \code{NA} values of the appropriate type.
File data is processed separately via \code{\link[=load_files_results]{load_files_results()}}.
}
\examples{
\dontrun{
# Load component results from JATOS JSON
component_result <- load_component_results(raw_component_data)
}

}
\seealso{
\code{\link[=load_files_results]{load_files_results()}} for details on file processing.
}
