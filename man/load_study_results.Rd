% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_study_results.R
\name{load_study_results}
\alias{load_study_results}
\title{Load Study Results from JATOS JSON}
\usage{
load_study_results(sr, parse_dates = TRUE)
}
\arguments{
\item{sr}{A list containing raw study result data from JATOS JSON.}

\item{parse_dates}{Logical. If \code{TRUE} (default), parses date columns to
\code{POSIXct} (via \code{lubridate::as_datetime()}) and duration to
\code{Duration} (via \code{lubridate::as.duration()}).
If \code{FALSE}, keeps them as numeric or character.}
}
\value{
A single-row tibble with columns:
\itemize{
\item \code{id}: Study result ID (numeric)
\item \code{uuid}: Unique study result identifier (character)
\item \code{study_code}: Study code (character)
\item \code{start_date}: When the study started (POSIXct or numeric)
\item \code{end_date}: When the study ended (POSIXct or numeric)
\item \code{duration}: Total study duration (Duration or character)
\item \code{last_seen_date}: When participant was last active (POSIXct or numeric)
\item \code{study_state}: Current state (e.g., "FINISHED", "ABANDONED") (character)
\item \code{message}: Optional message (character)
\item \code{worker_id}: Worker/participant ID (numeric)
\item \code{worker_type}: Type of worker (character)
\item \code{batch_id}: Batch ID (numeric)
\item \code{batch_uuid}: Batch UUID (character)
\item \code{batch_title}: Human-readable batch title (character)
\item \code{group_id}: Group ID for group-based studies (numeric)
\item \code{is_quota_reached}: Whether quota was reached (numeric, 0/1)
\item \code{component_results}: List column containing a tibble of component results
(as produced by \code{\link[=load_component_results]{load_component_results()}}), or \code{NULL} if no components
}
}
\description{
Transforms raw JATOS study result data into a tidy tibble format,
handling nested component results and optional date parsing.
It expects the results were imported from JSON using
\code{jsonlite::fromJSON(json, simplifyVector = F)}.
}
\details{
This function recursively processes nested component results by calling
\code{\link[=load_component_results]{load_component_results()}} on each component. Missing values in the input
are filled with \code{NA} values of the appropriate type.
}
\examples{
\dontrun{
# Load study results from JATOS JSON
study_result <- load_study_results(raw_study_data)
}

}
\seealso{
\code{\link[=load_component_results]{load_component_results()}} for details on component result processing.
}
