% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/is_valid_study_result.R
\name{is_valid_study_result}
\alias{is_valid_study_result}
\title{Validate Study Result Structure}
\usage{
is_valid_study_result(sr, verbose = FALSE)
}
\arguments{
\item{sr}{A tibble representing a study result from JATOS.}

\item{verbose}{Logical. If \code{TRUE}, prints information about failed checks.
Default is \code{FALSE}.}
}
\value{
A logical scalar: \code{TRUE} if all validation checks pass, \code{FALSE} otherwise.
}
\description{
Validates whether a study result has the expected structure, column names,
and data types as returned by \code{\link[=load_study_results]{load_study_results()}}.
}
\details{
Performs comprehensive validation of a study result including:
\itemize{
\item Verifies the input is a tibble
\item Checks for required numerical columns: \code{id}, \code{worker_id}, \code{batch_id},
\code{group_id}, \code{is_quota_reached}
\item Checks for required text columns: \code{uuid}, \code{study_code}, \code{study_state},
\code{message}, \code{worker_type}, \code{batch_uuid}, \code{batch_title}
\item Checks for required date/duration columns: \code{start_date}, \code{end_date},
\code{duration}, \code{last_seen_date}
\item Validates the \code{component_results} column is a list
\item Validates all nested component results are valid (via
\code{\link[=is_valid_component_result]{is_valid_component_result()}})
\item Ensures date columns are either POSIXct/Duration types OR numeric/character
(but not a mix)
}

When \code{verbose = TRUE}, failed checks are reported via \code{cli::cli_alert_info()}.
}
\examples{
\dontrun{
# Validate a study result
demo <- prepare_demo(nofile = TRUE)
txt <- demo$meta |> httr2::resp_body_string()
json <- jsonlite::fromJSON(txt, simplifyVector = FALSE)
sr_data <- json$data[[1]]$studyResults
r1 <- load_study_results(sr_data[[1]], parse_dates = TRUE)
r2 <- load_study_results(sr_data[[1]], parse_dates = FALSE)
is_valid_study_result(r1)

# Show which checks fail
r1$id <- "001"
is_valid_study_result(r1, verbose = TRUE)
}

}
\seealso{
\code{\link[=load_study_results]{load_study_results()}} which creates study results that pass
this validation; \code{\link[=is_valid_component_result]{is_valid_component_result()}} for component-level validation.
}
