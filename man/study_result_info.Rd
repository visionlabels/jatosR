% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/study_result_info.R
\name{study_result_info}
\alias{study_result_info}
\title{Parser of studyResults data}
\usage{
study_result_info(sr)
}
\arguments{
\item{sr}{list corresponding to a single \code{studyResults} entry, e.g.
\code{json$data[[1]]$studyResults[[1]]}}
}
\value{
Named list of size-one vectors: \code{id_sr}, \code{uuid}, \code{study_code},
\code{start_date_sr}, \code{end_date_sr}, \code{duration_sr} (computed as
\code{end_date_sr - start_date_sr}), \code{last_seen_date}, \code{study_state},
\code{worker_id}, \code{worker_type}, \code{batch_id}, \code{batch_uuid}, \code{batch_title},
\code{group_id}, and \code{component_results_node_count}.
}
\description{
Extracts a fixed set of fields from a single \code{studyResults} entry (as
parsed from JATOS JSON metadata) into a flat list. Missing fields fall
back to a typed \code{NA}, so the result is always a list of size-one vectors
that can be safely row-bound into a tibble, e.g. with \code{dplyr::bind_rows()}
as done in \code{process_results}.
}
\examples{
demo <- prepare_demo(nofile = TRUE)
json <- demo$meta |> httr2::resp_body_json()
sr <- study_result_info(json$data[[1]]$studyResults[[1]])
sr
}
